<?xml version="1.0" encoding="utf-8"?>

<!--
    Layout for displaying a single workspace item in a CardView.
    Provides elevation and rounded corners for a material look.
-->

<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    card_view:cardCornerRadius="8dp"
    card_view:cardElevation="4dp">

    <!-- TextView displaying the workspace name inside the card -->
    <TextView
        android:id="@+id/tvWorkspaceName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:textSize="18sp"
        android:text="Workspace Name" />

</androidx.cardview.widget.CardView>